extends layout

block content
    h1= title
    
    ul 
        each user in user_list
            li 
                a(href=user.url) IMSI: #{user.imsi}
                |   | Phone Number:  (#{user.number})
        else 
            li There are no users